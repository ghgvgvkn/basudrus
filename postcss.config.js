// Tailwind v4 reads through the Vite plugin; this file is kept only
// for editors that expect it and for environments that still use
// PostCSS directly. Safe to remove once the whole toolchain is on v4.
export default {
  plugins: {
    autoprefixer: {},
  },
};
