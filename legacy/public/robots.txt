User-agent: *
Allow: /

# Disallow authenticated / session-only paths from indexing (keeps the
# crawler on the public, educational landing content).
Disallow: /api/

Sitemap: https://basudrus.com/sitemap.xml
